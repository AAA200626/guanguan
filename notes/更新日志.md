# 更新日志

## 2026-07-18

### v0.0 — 项目初始化
- 新增：项目骨架搭建（7 文件，SOP 编号 005）
- 新增：需求确认（A+B 模式：存钱挑战 + 智能推荐）
- 新增：设计规范确定（薄荷青柠 + 苹果极简风）
- 新增：技术方案确定（Flutter + Riverpod + SQLite）

### v0.1 — 基础框架
- 新增：Flutter 3.44.6 环境搭建 + Android SDK
- 新增：主题系统（colors.dart + typography.dart）
- 新增：3-Tab 底部导航（首页/挑战/我的）
- 新增：首页静态 UI（资产卡片 + 进度环 + 计划 + 里程碑）
- 新增：挑战页静态 UI（智能推荐横幅 + 方法卡片列表）
- 新增：我的页静态 UI（统计 + 徽章 + 设置）

### v0.2 — 存钱方法库
- 新增：6 种存钱法数据模型 + 计算引擎
- 新增：智能推荐算法（收入驱动 + 偏好筛选）
- 新增：方法设置页（数据预览 + 一键创建计划）
- 新增：Riverpod 状态管理

### v0.3 — 打卡系统
- 新增：挑战详情页（打卡 + 进度 + 记录）
- 新增：首页实时数据联动
- 新增：创建计划 → 直接跳转打卡页
- 优化：首页 FAB 按钮

### v0.4 — 数据持久化
- 新增：SQLite 数据库（plans/records/badges 三表）
- 新增：打卡记录 CRUD + 启动时自动加载
- 新增：每日通知提醒（8:00-22:00 可选）
- 新增：timezone 时区支持

### v0.5 — 个人中心
- 新增：自定义头像（从相册选取）
- 新增：自定义昵称
- 新增：35 种成就徽章（可折叠）
- 新增：连续打卡 / 存钱金额 / 行为特征徽章自动判定
- 删除：里程碑卡片、设置项（移到首页）
- 删除：首页 🫙 图标、铃铛、设置按钮

### v0.6 — UI 迭代优化
- 优化：全局毛玻璃质感（半透明卡片 + 边框）
- 优化：首页紧凑化（一屏不滚动）
- 优化：挑战页紧凑化（间距/内边距缩减）
- 优化：存钱法分两组展示（每月存钱 / 每日每周存钱）
- 新增：月存法显示「月存¥xx」、日周法显示「日均¥xx」
- 新增：日周法首页显示「今日¥xx」可点改、月存法只显示打卡
- 新增：详情页日历（日周法=当月日历打卡上色、月存法=12月块）
- 删除：详情页不再跳转、首页直接打卡

### v0.7 — 计算方法重构
- 重构：所有金额基于月收入动态计算，不写死
- 新增：13 种存钱法（365天/52周/月份倒数/星期循环/333/1234/10%/12存单/6罐/取整/梦想/假装/对赌）
- 优化：存入金额 = 月储蓄率 ÷ 当月天数，每天固定
- 修复：中文路径 Gradle 构建
- 修复：Android desugaring 配置
- 优化：自定义 App 图标（flaticon 存钱罐图标）

### v0.8 — 修复 & 收尾
- 修复：智能推荐红屏（BottomSheet → AlertDialog）
- 修复：按钮无响应（首页所有按钮接线）
- 修复：方法设置页 ConsumerStatefulWidget 乱码
- 清理：删除未使用的组件和导入
- 优化：打卡过的时间日历上色

### v0.9 — 乱码修复 & 精度优化
- 修复：月存法日历显示乱码（$now → ${now.year}）
- 修复：整数取整法页面乱码（Write 工具重写文件，UTF-8 干净编码）
- 优化：333存钱法金额显示两位小数（月存/年存/首存）
- 优化：日周法首页显示「今日¥xx」可点改，月存法只显示打卡按钮
- 优化：详情页日历按方法类型区分（日周法=天日历，月存法=月份块）
